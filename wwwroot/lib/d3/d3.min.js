// Function to create a basic SVG element
function createSVG(containerId, width, height) {
    return d3.select(containerId)
        .append("svg")
        .attr("width", width)
        .attr("height", height);
}

// Function to draw the strike zone
function drawStrikeZone(svg, x, y, width, height, color = "red", opacity = 0.5) {
    svg.append("rect")
        .attr("x", x)
        .attr("y", y)
        .attr("width", width)
        .attr("height", height)
        .attr("fill", color)
        .attr("opacity", opacity);
}

// Example usage
document.addEventListener('DOMContentLoaded', function () {
    var svg = createSVG("#strikeZoneViz", 500, 500); // Create SVG element
    drawStrikeZone(svg, 100, 100, 200, 250); // Draw the strike zone
    
    // Example of fetching game data from API
    fetch('/api/games/12345')
        .then(response => response.json())
        .then(data => {
            // Process the data and draw visualization based on the data
            // e.g., draw pitches on the strike zone
        })
        .catch(error => console.error("Error fetching game data:", error));
});